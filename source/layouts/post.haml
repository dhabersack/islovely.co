- wrap_layout :scaffolding do
  %article
    %header
      %h2= current_page.data.title

      %p= current_page.data.date.strftime('%B %d, %Y')

    = yield

    %footer
      %p
        If you read this far, consider following me on Twitter: #{ link_to '@dhabersack', 'http://twitter.com/dhabersack' }.
